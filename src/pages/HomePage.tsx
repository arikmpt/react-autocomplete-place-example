import { HomeContainer } from "@containers"
import { PublicLayout } from "@layouts"

const HomePage = () => (
    <PublicLayout>
        <HomeContainer />
    </PublicLayout>
)

export default HomePage